"""
1. test init if ProfileStore doesnt get a base_name

2. test write if directory "profile_store" exists
3. test write if directory profile_store does not exist
4. test write to same timestamp if overwrites
"""
