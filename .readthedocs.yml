version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.9"
  jobs:
    pre_build:
      - curl -sSL https://install.python-poetry.org | python -
      - which python
      - which pip
      - cd python && /home/docs/.local/bin/poetry config virtualenvs.create false --local && /home/docs/.local/bin/poetry install -E docs
      - cd python && /home/docs/.local/bin/poetry run which python
      - pip list

sphinx:
   configuration: python/docs/conf.py
